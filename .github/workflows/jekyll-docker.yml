name: outputs demo
on: push
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: output the version
        id: step1
        run: echo "version=0.01" >> $GITHUB_OUTPUT
      - name: print output
        run: echo " the output version is- ${{steps.step1.outputs.version}}"
        
        
    
