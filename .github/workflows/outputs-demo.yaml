name: demo outputs
on: push
jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - name: step output
      run: echo "version = 0.01" >> $GITHUB_OUTPUT
    - name: print outputs:
      run: echo " the output version is ${{GITHUB_OUTPUT}}"
      
