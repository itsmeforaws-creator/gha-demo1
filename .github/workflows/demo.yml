 name: Hello World
 on: 
   push: 
     branches: 
       - main
      #-release/** It will trigger the workflow when there is a push to any branch that starts with "release/"
   pull_request:
      branches:
        - main  
 jobs:
   Build:
     runs-on: ubuntu-latest
     steps:
       - name: build step
         run: |
           echo "Hello it's step Build step, variable ${{vars.MY_NAME}} secrete ${{secrets.MY_PASSWORD}}"
          
         
   
   
