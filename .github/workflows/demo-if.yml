name: demo if
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: if demo on main
        if: github.ref == 'refs/heads/main'
        run: echo "this is true condition for main branch"


        
